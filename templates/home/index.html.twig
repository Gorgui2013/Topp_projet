{% extends 'base.html.twig' %}

{% block body %}

    <div class="container py-3">
        <div class="row">
            <div class="col-sm-6">
                <h4>Projets</h4>
            </div>
            <div class="col-sm-6" align="right">
                <a href="#newprojet" class="btn btn-sm btn-outline-secondary" data-bs-toggle="modal"><i class="fas fa-plus"></i> Projet</a>
            </div>
            {% for message in app.flashes('success') %}
                <div class="alert alert-success">
                  {{ message }}
              </div>
          {% endfor %}
      </div>
      <hr>
      <div class="row">
        {% include 'home/projets.html.twig' %}
    </div>
</div>

</div>
</div>

{% include 'home/newprojet.html.twig' %}
{% endblock %}
